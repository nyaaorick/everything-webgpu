{
  "name": "everything-webgpu-example-bare",
  "private": true,
  "type": "module",
  "description": "Gate A — a bare Vite page loads a prebuilt model and generates, consuming the package through its exports map.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "everything-webgpu": "file:../.."
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
